<h1><%= @category.name %></h1>
<p>The following articles were published in this category:</p>
<% @category_articles.each do |article| %>
  <div>
    <h3><%= link_to article.title, article %></h3>
      <p>Published on <%= article.created_at.strftime('%b %d, %Y') %></p>
    <p><%= truncate(article.content, length: 200 ) { link_to "continue", article } %></p>
  </div>
<% end %>